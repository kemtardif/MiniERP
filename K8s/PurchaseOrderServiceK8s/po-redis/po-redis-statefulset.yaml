apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: po-redis
spec:
  selector:
    matchLabels:
      app: po-redis
  serviceName: po-redis-srv
  replicas: 1
  template:
    metadata:
      labels:
        app: po-redis
    spec:
      initContainers:
      - name: init-redis
        image: redis:latest
        command:
        - bash
        - "-c"
        - |
          set -ex
          cp /mnt/po-redis.conf /etc/po-redis.conf        
        volumeMounts:
        - name: redis-claim
          mountPath: /etc
        - name: config-map
          mountPath: /mnt/
      containers:
      - name: po-redis
        image: redis:latest
        ports:
        - containerPort: 6379
          name: redis-ss
        command:
          - redis-server
          - "/etc/po-redis.conf"
        volumeMounts:
        - name: redis-data
          mountPath: /data
        - name: redis-claim
          mountPath: /etc
      volumes:
        - name: config-map
          configMap:
            name: po-redis-config 
  volumeClaimTemplates:
  - metadata:
      name: redis-claim
    spec:
      storageClassName: po-redis-local-storage
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 150Mi
  - metadata:
      name: redis-data
    spec:
      storageClassName: po-redis-local-storage
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 150Mi


